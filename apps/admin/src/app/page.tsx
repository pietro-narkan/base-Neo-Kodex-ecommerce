export default function Home() {
  return (
    <main className="min-h-screen flex items-center justify-center">
      <div className="text-center space-y-2">
        <h1 className="text-3xl font-bold">Neo-Kodex Admin</h1>
        <p className="text-gray-500">Base Fase 0 — listo para Fase 1 (auth + CRUDs)</p>
      </div>
    </main>
  );
}
